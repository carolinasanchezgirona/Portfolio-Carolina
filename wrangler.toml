name = "portfolio-carolina"
main = "./worker.ts"
compatibility_date = "2026-09-21"
keep_vars = true

[build]
command = "npm run build"

[assets]
directory = "./out"
binding = "ASSETS"
run_worker_first = [ "/api/stripe/webhook", "/api/stripe/webhook/" ]

[secrets]
required = [ "STRIPE_WEBHOOK_SECRET" ]

# Stripe webhook runs in the Worker; all other requests are served from static assets.
# keep_vars preserves dashboard-managed variables and secrets across Wrangler deploys.
# secrets.required makes deploy fail clearly if the runtime secret is missing.
