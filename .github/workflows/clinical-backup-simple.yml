name: Copia clínica simple

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Comprobar que el nuevo sistema arranca
        run: |
          echo "Nuevo sistema de copia clínica iniciado correctamente."
